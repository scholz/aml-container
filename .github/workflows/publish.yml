name: publish

on: [push]

jobs:
  publish-aml-container:
    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v2

    - name: Login to GitHub Container Registry
      uses: docker/login-action@v1
      with:
        registry: ghcr.io
        username: ${{ github.actor }}
        password: ${{ secrets.GITHUB_TOKEN }}

    - name: Build the hello-world Docker image
      run: |
        # init project; don't use recursive to save space
        git clone --depth 1 https://github.com/khadas/aml_npu_sdk.git
        rm -rf aml_npu_sdk/.git
        git clone --depth 1 --branch ksnn_convert https://gitlab.com/khadas/neural_network_large_file.git aml_npu_sdk/python/
        rm -rf aml_npu_sdk/python/.git
        git clone --depth 1 --branch aml_npu_convert_tool https://gitlab.com/khadas/neural_network_large_file.git aml_npu_sdk/bin/
        rm -rf aml_npu_sdk/bin/.git

        ls  -al
        du -sh .
        docker build . --tag ghcr.io/scholz/aml-container:latest
        docker push ghcr.io/scholz/aml-container:latest

